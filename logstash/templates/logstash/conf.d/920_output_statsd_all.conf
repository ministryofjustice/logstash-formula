output {

    statsd {
      increment => [
        "%{host}.events.type.%{type}",
        "%{host}.events.all"
      ]
      sender => "per-host"
    }

    statsd {
      increment => [
        "events.type.%{type}",
        "events.all"
      ]
      sender => "all"
    }

}
