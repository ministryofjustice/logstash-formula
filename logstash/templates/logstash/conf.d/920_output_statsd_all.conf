output {

    statsd {
      increment => [
        "%{host}.indexer.events.per-type.%{type}",
        "%{host}.indexer.events.all"
      ]
      sender => "per-host"
    }

    statsd {
      increment => [
        "indexer.events.per-type.%{type}",
        "indexer.events.all"
      ]
      sender => "all"
    }

}
